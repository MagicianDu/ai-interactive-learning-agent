# Database Index Lesson

This directory contains the design artifact for the first MVP lesson:

```text
Why database indexes make queries faster
```

Current status:

- Lesson design object exists.
- React implementation has not started.

Future expected files:

- `lesson-design.json` as source design input
- `lesson.json` as implementation-ready lesson data
- `Lesson.tsx` only after implementation begins

